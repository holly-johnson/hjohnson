import { Routes } from '@angular/router';

/**
 * Production stand-in for `unpublished.routes.ts`, swapped in by `fileReplacements`
 * in angular.json. No routes and, more to the point, no dynamic imports: the
 * unpublished components are left out of the production bundle entirely, so
 * unfinished work is never uploaded, not even as an unlinked chunk.
 */
export const unpublishedRoutes: Routes = [];
