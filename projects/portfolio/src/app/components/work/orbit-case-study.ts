import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

/**
 * Orbit, deliberately a stub. The work did not ship and the full write-up is not
 * written, so the page says that rather than standing in for a case study.
 * The reasoning lives in Helios §06, which this page links to.
 */
@Component({
  selector: 'app-orbit-case-study',
  imports: [RouterLink],
  templateUrl: './orbit-case-study.html',
})
export class OrbitCaseStudy {}
