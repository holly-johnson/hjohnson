import { Routes } from '@angular/router';

/**
 * Production stand-in for `field-notes.routes.ts`, swapped in by `fileReplacements`
 * in angular.json. No routes and, more to the point, no dynamic imports: the journal
 * components are left out of the production bundle entirely, so unfinished writing
 * is never uploaded, not even as an unlinked chunk.
 */
export const fieldNotesRoutes: Routes = [];
